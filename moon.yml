$schema: 'https://moonrepo.dev/schemas/project.json'
layer: 'library'
tags: ['mojo']

tasks:
  check:
    command: 'pixi run test'
    toolchain: 'system'
    inputs: ['src/**/*', 'pixi.toml', 'pixi.lock']
